<h3>LearningDays</h3>

<ul class="learning-days">
    @for (int i=0; i<7; i++)
    {
        <li>
            <input type="checkbox" id="@Week.DAYNAMES[i]" @bind="@days[i]" />
            <lable for="@Week.DAYNAMES[i]">@Week.DAYNAMES[i]</lable>
        </li>
    }
</ul>

@code 
{
    bool[] days = new bool[7];
}
